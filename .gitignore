node_modules/
data/
*.db

# 환경변수 (절대 GitHub에 올리면 안 됨)
.env
.env.local
.env.*.local
.env.production
*.env

# Claude Code 로컬 설정
.claude/

# Claude Code 프롬프트 파일
/CLAUDE_CODE_PROMPT_*.md

# Prisma 생성 파일
/generated/prisma

# 백업 파일
*.backup

# Docker
.docker/
*.log

# 컨테이너 빌드 캐시
.docker-cache/
# 로컬 docker-compose 오버라이드 (포트 재매핑 등 — 환경별, 커밋 제외)
docker-compose.override.yml
# nginx 렌더 결과 (환경별 산출물 — 커밋 제외)
nginx/conf.d/hrpms.conf
